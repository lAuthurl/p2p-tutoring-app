Place the Poppins font files here with EXACT filenames (case-sensitive on some systems):

- Poppins-Regular.ttf
- Poppins-SemiBold.ttf
- Poppins-Bold.ttf

Notes:

- Do NOT add empty or placeholder .ttf files — they will break font loading.
- After adding the files, run `flutter pub get` and rebuild the app.
- Use the font in code with `TextStyle(fontFamily: 'Poppins', ...)` instead of `GoogleFonts`.
